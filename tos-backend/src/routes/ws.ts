export interface WSPayload {
  msgType: string;
  payload: any;
}


export const wsEventHandler = (payload: WSPayload) => {
  if (payload.msgType === "") {

  } else if (payload.msgType === "") {

  }
}
